import { StyleSheet, Text, View } from "react-native";

export default function ExpenseContext() {
  return (
    <View>
      <Text>expenseContext</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
